# Copia este archivo como  .env  (sin extensión .example) y pon tu clave.
# El archivo .env NO se sube a GitHub (está en .gitignore), así que tu clave
# queda solo en tu ordenador.

# Clave de la API de Anthropic (para traducción y resumen con Claude).
# Consíguela en https://console.anthropic.com/keys
ANTHROPIC_API_KEY=sk-ant-aqui-tu-clave
